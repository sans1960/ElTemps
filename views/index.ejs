<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>El Temps</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  </head>

  <body>
    <% if (data === '') { %>
    <div class="container mt-5 col-lg-6">
      <p class="display-4 text-center">El Temps -> <a href="https://websans.app" target="_blank">websans</a></p>
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Busca el temps a una localitat</h5>
          <form action="/" method="post">
            <div class="form-group">
              <input type="text" class="form-control" name="location" placeholder="Location">
              
            </div>
            <button type="submit" class="btn btn-primary">Envia</button>
          </form>
        </div>
      </div>
    </div>
    <% } else if (data === '0') { %>
    <div class="container mt-5">
      <div class="card">
        <div class="card-body">
          <p class="card-title">Oops! Lost in space🚀, try locations from Earth🌏 !</p>
        </div>
      </div>
      <div class="text-center">
        <a href="/" class="btn btn-primary mt-5 mx-auto">Nou lloc</a>
      </div>
    </div>
    <% } else { %>
    <div class="container mt-5">
      <p class="display-4 text-center">El Temps -> <a href="https://websans.app" target="_blank">websans</a></p>
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">El Tems a <%= data.name %>, <%= data.sys.country %></h5>
          <p class="text-muted">Lat: <%= data.coord.lat %>, Lon: <%= data.coord.lon %></p>
          <img class="float-right" src="http://openweathermap.org/img/wn/<%= data.weather[0].icon %>@2x.png" alt="icon">
          <p class="display-1"><%= data.main.temp%><sup>o</sup>C</p>
          <span class="text-muted">Sensació  <%= data.main.feels_like %><sup>o</sup>C</span>
          <h5 class="text-capitalize font-weight-bold"><%= data.weather[0].description %></h5>
          <div class="overflow-x">
            <span class="pr-5 inf"><i class="fas fa-wind"></i> Vent <%= data.wind.speed %> m/sec</span>
            <span class="pr-5 inf"><i class="fas fa-paper-plane"></i> Pressió <%= data.main.pressure %> mb</span>
            <span class="pr-5 inf"><i class="fas fa-tint"></i> Humitat <%= data.main.humidity %>%</span>
            <span class="pr-5 inf"><i class="fas fa-eye"></i> Visibilitat <%= data.visibility/1000 %> km</span>
            <span class="pr-5 inf"><i class="fas fa-cloud"></i> Nubolositat <%= data.clouds.all %>%</span>
          </div>
        </div>
      </div>
      <div class="text-center">
        <a href="/" class="btn btn-primary mt-5 mx-auto">Nou Lloc</a>
      </div>
    </div>
    <% } %>

  </body>

</html>
